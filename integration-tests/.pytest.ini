[pytest]
addopts =
    --code-highlight=yes
    --color=yes
    --show-capture=no
    --strict-config
    --strict-markers
    --tb=short
    --verbose

console_output_style = classic

env =
    D:CLP_BUILD_DIR=../build
    D:CLP_CORE_BINS_DIR=../build/core
    D:CLP_PACKAGE_DIR=../build/clp-package
    D:INTEGRATION_TESTS_PROJECT_ROOT=./
    D:PRESTO_DEPLOYMENT_DIR=../tools/deployment/presto-clp

log_file_mode = a
log_file_level = INFO
log_file_format = %(asctime)s.%(msecs)03d %(levelname)s %(message)s
log_file_date_format = %Y-%m-%d %H:%M:%S

markers =
    admin_tools: mark tests that test admin tools
    archive_manager: mark tests that test archive-manager
    clp: mark tests that use the CLP storage engine
    clp_json: mark tests that use the clp-json package
    clp_presto: mark tests that use the clp-presto package
    clp_s: mark tests that use the CLP-S storage engine
    clp_text: mark tests that use the clp-text package
    compression: mark tests that test compression
    core: mark tests that test the CLP core binaries
    dataset_manager: mark tests that test dataset-manager
    package: mark tests that use the CLP package
    search: mark tests that test search
    startup: mark tests that test startup
